spring:
  profiles:
    active: batch

  datasource:
    url: jdbc:mysql://localhost:3306/hangover?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: hs8569710!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  task:
    scheduling:
      pool:
        size: 3

batch:
  enabled: true

logging:
  level:
    root: info
    org.hibernate.SQL: debug